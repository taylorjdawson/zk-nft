

export const isApprovedForAll = (address) => true && address
export const approveForAll = (address) => true && address
export const addDAOIdentity = (groupId = 0, identityCommitment, tokenId) => {
  console.log(groupId, identityCommitment, tokenId)
}
